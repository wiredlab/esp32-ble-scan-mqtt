

BLE advertisement sniffer

Continuously sniff BLE adv packets
	Timestamp
	received MAC
	payload
	name if available
	RSSI if available
	TXpower if available

Logging
	MQTT
		topic: esp32-sniffer/<MAC>/status


Notes
	uSD card conflicts with builtin LED pin, CANNOT use both


